# requirements.txt — grown one section at a time (project standard).
# Every implementation that needs a package adds it here in the SAME change.

# ── core / logging ─────────────────────────────────────────────────────────
# standard library only (logging, pathlib, datetime).

# ── foundations: schemas, profiler ─────────────────────────────────────────
pandas

# ── sandbox: run model code in a subprocess (charts via matplotlib) ─────────
matplotlib

# ── model layer: OpenAI GPT client ─────────────────────────────────────────
openai>=1.0
python-dotenv

# ── server: FastAPI wrapper over the chat path ─────────────────────────────
fastapi
uvicorn[standard]
python-multipart          # multipart CSV upload
httpx                     # TestClient (and a handy HTTP client)
