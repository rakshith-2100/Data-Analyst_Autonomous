"""CHECK [agent] — judge whether the result answers the question.

Phase 4: implement build_messages/parse + validate. Stub for now.
"""
NAME = "CHECK"
MODEL_TIER = "cheap"


def run(state, ctx):
    """Produce this action's output (model call). Stub — returns None until Phase 4."""
    return None


def validate(output, ctx):
    """Judge the output -> Verdict. Stub — returns None until Phase 4."""
    return None
