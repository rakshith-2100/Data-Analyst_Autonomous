"""ENRICH [sys] — build targeted error context for the error_kind. Plain code, no model.

Phase 4: implement. Stub for now.
"""
NAME = "ENRICH"


def run(state, ctx):
    """build targeted error context for the error_kind. Stub — returns None until Phase 4."""
    return None


def validate(output, ctx):
    """Stub — returns None until Phase 4."""
    return None
