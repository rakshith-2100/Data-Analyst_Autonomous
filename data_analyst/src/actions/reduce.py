"""REDUCE [agent] — rewrite the cell to sample/chunk (timeout/memory).

Phase 5: implement build_messages/parse + validate. Stub for now.
"""
NAME = "REDUCE"
MODEL_TIER = "cheap"


def run(state, ctx):
    """Produce this action's output (model call). Stub — returns None until Phase 5."""
    return None


def validate(output, ctx):
    """Judge the output -> Verdict. Stub — returns None until Phase 5."""
    return None
